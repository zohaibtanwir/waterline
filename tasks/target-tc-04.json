{
  "task_id": "target-tc-04",
  "repo_url": "https://github.com/zohaibtanwir/target-tripconcierge.git",
  "branch": "main",
  "image": "waterline/sandbox-base:v2",
  "mount": "rw",
  "prompt": "The agents package has seven task-builder functions named make_*_task. They are currently only covered indirectly. Write direct unit tests for them: each builder should have a test asserting how it is wired and configured, following the conventions already used by the existing tests in that package. Done means both of: (1) the full agents and mcp_server suites are green including your new tests, and (2) the new tests exist as changes to files inside the repository working tree. Do not modify existing tests or source code to make your tests pass; if a builder appears untestable as written, report that rather than changing it.",
  "env": {
    "ANTHROPIC_MODEL": "execute",
    "KEEL_TEST_CMD": "cd /workspace/repo/agents && uv run pytest -q && cd /workspace/repo/mcp_server && uv run pytest -q"
  },
  "caps": {
    "timeout_seconds": 2700,
    "budget_usd": 5
  }
}
